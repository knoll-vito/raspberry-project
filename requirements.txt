# --- Core ML stack (Student) ---
lightgbm>=4.3.0
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# --- Teacher / API client ---
openai>=1.30          # DeepSeek-V4 兼容 OpenAI 接口
httpx>=0.27
tenacity>=8.2         # 退避重试
python-dotenv>=1.0
PyYAML>=6.0
tqdm>=4.66

# --- Inference / 部署 ---
# 推理时仅需 lightgbm + joblib + numpy，无需 openai

# --- 开发辅助（可选） ---
matplotlib>=3.7       # 评估可视化
